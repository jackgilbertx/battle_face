import { render, screen } from '@testing-library/react';
import App from './App';

test('renders Banner header text', () => {

  // todo - add tests if you have time

  // render(<App />);
  // expect(screen.getByRole('heading')).toHaveTextContent('Banners');
});
